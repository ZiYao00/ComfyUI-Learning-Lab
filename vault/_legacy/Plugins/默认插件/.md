
[GradientDeepShrink](https://github.com/kinfolk0117/ComfyUI_GradientDeepShrink) 

![[Pasted image 20241012224915.png]]
block_number：`在模型中应用降采样操作的块`
downscale_factor：`输入被降采样的因子`
start_percent 和 end_percent：`定义缩小比例因子从 1/downscale_factor 线性插值到 1 的范围`
downscale_after_skip：`一个布尔标志，决定是否在块中的跳过连接之后应用降尺度操作`
downscale method：`放大算法`
upscale method：`缩放算法`

[[【算法】图像缩放]]  
